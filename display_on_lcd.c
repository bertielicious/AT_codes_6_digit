#include <xc.h>
#include <stdbool.h>
#include "config.h"
#include "pulse_E.h"



/* Function void display_on_lcd (uchar data, bool inst_dat) accepts an 8-bit argument, splits it into two 4 bit nibbles
 which are then written to the 4-bit LCD interface via PORTA and function pulse_E. 
 * inst_dat tells the LCD data is a LCD instruction or data*/
void display_on_lcd (uchar data, bool inst_dat)
{
  
     
}
